# Frontend environment variables (Vite).
# Copy to `.env` for local dev. On Render these must be set at BUILD time.
#
# Only the public Google Client ID belongs here — it is baked into the bundle
# and is safe to expose. The Client SECRET must NEVER appear in the frontend.
VITE_GOOGLE_CLIENT_ID=
